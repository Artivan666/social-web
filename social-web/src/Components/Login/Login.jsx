import s from './Login.module.css'
import ss from '../../App.module.css'

export const Login = () => {
  return (
    <div className={s.login + ' ' + ss.os + ' ' + ss.cp}>
      <p>Login</p>
    </div>
  )
}
